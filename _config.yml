# -----------------------------------------------------------------------------
# Site
# -----------------------------------------------------------------------------
title: blank
first_name: Writabrata
middle_name:
last_name: Bhattacharya
description: >
  Applied Scientist | LLMs, Speech Deep Learning, MLOps & World Models
keywords: machine-learning, deep-learning, LLM, speech, TTS, STT, MLOps, world-models, reinforcement-learning
lang: en
icon: ⚛️
url: https://sudo-kaneki.github.io
baseurl:

# -----------------------------------------------------------------------------
# Blog
# -----------------------------------------------------------------------------
blog_name: Writabrata's Blog
blog_description: Thoughts on ML, AI, Mathematics & beyond
permalink: /blog/:year/:title/

# -----------------------------------------------------------------------------
# Build
# -----------------------------------------------------------------------------
markdown: kramdown
highlighter: rouge
kramdown:
  input: GFM
  syntax_highlighter_opts:
    css_class: "highlight"
    block:
      line_numbers: false

plugins:
  - jekyll-email-protect
  - jekyll-feed
  - jekyll-link-attributes
  - jekyll-minifier
  - jekyll-paginate-v2
  - jekyll-sitemap
  - jemoji

# The Gemfile's :jekyll_plugins group still declares jekyll-3rd-party-libraries
# (auto-required by Bundler regardless of the plugins: list above). Its
# after_init hook unconditionally iterates this key. An empty hash is a
# harmless no-op. Task 7 removes the gem from the Gemfile; this stub can be
# deleted then.
third_party_libraries: {}

include: ["_pages"]

exclude:
  - bin/
  - docs/
  - CONTRIBUTING.md
  - CUSTOMIZE.md
  - Dockerfile
  - docker-compose.yml
  - docker-compose-slim.yml
  - FAQ.md
  - Gemfile
  - Gemfile.lock
  - INSTALL.md
  - LICENSE
  - lighthouse_results/
  - package.json
  - package-lock.json
  - QUICKSTART.md
  - README.md
  - requirements.txt
  - SEO.md
  - TROUBLESHOOTING.md

defaults:
  - scope:
      path: ""
      type: "posts"
    values:
      layout: "article"

# -----------------------------------------------------------------------------
# Minification (production only)
# -----------------------------------------------------------------------------
jekyll-minifier:
  exclude: ["robots.txt"]
  compress_javascript: true
  compress_css: true
